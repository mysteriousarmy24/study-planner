org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Use the Flutter-provided Kotlin support instead of the legacy plugin path.
android.builtInKotlin=true
# Keep the modern Gradle DSL enabled for the current Flutter toolchain.
android.newDsl=true
